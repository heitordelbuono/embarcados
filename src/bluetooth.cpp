#include "bluetooth.h"
#include "esp_log.h"

static const char* TAG = "BT";

void Bluetooth::iniciaConexao() {
    // TODO (proximos passos): iniciar BLE "Mesa PID".
    ESP_LOGI(TAG, "bluetooth (stub)");
}

bool Bluetooth::temComando() {
    return false;
}
